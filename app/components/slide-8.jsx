import React from 'react';

export default function Slide8() {
  return (
    <section  style={{textAlign:'left'}}>
      <h1 className="r-fit-text">Conclusion</h1>
      <p className="r-fit-text">After discussing both approaches we came to the conclusion that both would work. It wouldn't be a bad idea to have a hybrid in which we would do the grouping first and then apply the nearest neighbor once inside the grouping for the deliveries. We still haven’t fully chosen if we do a hybrid or the approach(Y) because we would like to explore the algorithms and actually test them to see what works best for us.</p>
    </section>
  );
}
